# docs/reference — legacy layer

`docs/reference/` сохранён только как legacy/archive-слой.

Правила:
- это **не** canonical слой документации;
- это **не** active working layer;
- при любых расхождениях приоритет у `README.md` и актуальных документов в `docs/`.
